---
title: Example Draft Article
date: 2026-03-20
---

This article is a draft. It is defined with `draft: true` in `src/articles/index.ts`, which means it is excluded from the articles list by the `.filter((a) => !a.draft)` step.

To publish it, remove `draft: true` (or set it to `false`) in `index.ts`.
